/* This file sources its data from the matching hero.json. Edit hero.json instead. */
import heroJson from './hero.json';

// Re-export the JSON data to keep compatibility with JS imports
export const hero = heroJson.hero;
